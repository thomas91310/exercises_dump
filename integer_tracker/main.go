package main

func main() {
	// it := NewIntegerTracker()
	// it.track(1)
	// it.track(0)
	// fmt.Println("max so far ", it.getMax())
	// fmt.Println("min so far ", it.getMin())
	// fmt.Println("mean so far ", it.getMean())
	// fmt.Println("mode so far ", it.getMode())
	// it.track(3)
	// it.track(1)
	// fmt.Println("max now ", it.getMax())
	// fmt.Println("min now ", it.getMin())
	// fmt.Println("mean now ", it.getMean())
	// fmt.Println("mode now ", it.getMode())
}
